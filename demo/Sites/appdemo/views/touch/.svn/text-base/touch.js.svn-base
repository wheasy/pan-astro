Mt.defineView('touch', function(scope, ele, params) {
	//params.params为问号传参数
	var M = arguments[arguments.length - 1];
	scope.$on('loaded',function(){
		scope.$mask();
		// var self = ele.one('#touch');
		// // 防止滚动事件重复绑定
		// self.plug([    
		//     {
		//         fn:M.Plugin.MotoScroll
		//     }
		// ])
	})
});