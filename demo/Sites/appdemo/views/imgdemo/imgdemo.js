Mt.defineView('imgdemo', function(scope, ele, params) {
	//params.params为问号传参数
	//var M = arguments[arguments.length - 1];
	// scope.$on('loaded_lazy',function(){
	// 	debugger;
	// 	M.inScreen.watch(ele.all('img'));
	// })
	
});